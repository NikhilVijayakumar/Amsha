# AI Audit Configuration Template
# Copy this to .ai_audit.env and fill in your API keys

# LLM Provider: "lmstudio", "gemini", or "openai"
LLM_PROVIDER=lmstudio

# LM Studio Configuration (OpenAI-compatible API)
LM_STUDIO_BASE_URL=http://localhost:1234/v1
LM_STUDIO_MODEL=local-model
LM_STUDIO_API_KEY=not-needed

# Gemini Configuration  
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

# OpenAI Configuration
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Temperature for code review (0.0-1.0)
TEMPERATURE=0.3

# Max tokens for response
MAX_TOKENS=1000
