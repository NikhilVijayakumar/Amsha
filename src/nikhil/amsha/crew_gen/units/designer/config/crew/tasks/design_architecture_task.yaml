task:
  description: >
    Analyze the request: "{request}".
    Define the file paths for TWO crews:
    1. The Feature Crew (The Doer).
    2. The Evaluation Crew (The Judge).
    
    Identify the target directory: "{target_dir}".
  expected_output: >
    A JSON list of file paths that need to be created (agents.yaml, tasks.yaml, app.py for both crews).



